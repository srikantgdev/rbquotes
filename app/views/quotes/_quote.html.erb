<%# app/views/quotes/_quote.html.erb %>

<%= turbo_frame_tag quote do %>
  <div class="mb-2">
    <div><%= quote.name %></div>
    <div>
      <%= link_to "Delete", quote_path(quote), class: "btn btn-sm btn-warning" %>
      <%= link_to "Edit", edit_quote_path(quote), class: "btn btn-sm btn-info" %>
    </div>
  </div>
<% end %>